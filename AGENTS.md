# Eva 模块化原型

## 开工前强制阅读与合并纪律

- 所有 AI 在分析后开始修改前，必须完整阅读本文件、`CONTRIBUTING.md`、[AI 开发与合并验收规范](docs/AI开发与合并验收规范.md)及对应业务规范；不能以历史记忆、文件名或局部搜索代替。规范是实施约束，不是交付后补查材料。
- 开工先说明本次适用规范与保留的产品行为；用户最新明确决定优先。无需用户决策的兼容修复自行完成；改变功能取舍、身份边界或设计范围必须确认。
- 合并必须对照已发布基线逐项保留功能；同事未同步的功能不能当作有意删除。禁止未经逐段分析使用整文件 ours/theirs 解决业务冲突。
- 恢复旧代码必须同时核对 DOM、CSS、状态、路由、监听器与入口装配的兼容性；构建成功不等于恢复成功。
- 不得通过删除或弱化测试让错误实现通过。用户未禁止测试时按 CONTRIBUTING.md 的改动分级运行相关自动化及适用的本地浏览器验收；禁止测试时如实标注未验收，不得声称全部功能正常。

## 现行架构（接手前必读）

一套模块化源码、一个浏览器入口、一条构建链、一个原生路由出口；GitHub、Vercel 与本地预览读取同一套文件，不存在需人工同步的第二份 HTML。发布链路：本地 `npm start`（build → `dist/` HTTP 预览，禁止 `file://`）→ 验证后 push → GitHub `main` → Vercel 自动构建。

构建链严格单向：`npm run build` 读取 `vendor/eva-legacy-runtime.js`，按 009-4→009-8 固定顺序执行补丁并生成唯一产物 `dist/vendor/eva-runtime.module.js`；锚点不匹配立即构建失败。`dist/index.html` 依次加载 009-0 至 009-3 Demo 数据、010 注册桥与该运行时，之后进入 React HashRouter → ProtectedLayout → Layout → Outlet（渲染当前路由的唯一 Page）。

一级页面宿主（均挂在 Outlet；全局唯一 `Topbar` 与选中态由 URL 推导的 `Sidebar`）：`PersonalEvaPage`（/guid、/conversation/:id）、`TeamMessagesPage`（/messages，含关注/最近、我的 AI 模式）、`ProjectsPage`（/collab）、`ContactsPage`（/contacts）、`DrivePage`（/drive）、`WorkboardPage`、`DigitalEmployeesPage`、`ConnectionCenterPage`（/eva-stub/…）。

`010-native-page-registry.js` 是非 React 页面与 React Route 宿主之间的注册桥，并在路由离开时执行 cleanup。它不是第二个页面控制器，禁止由它创建 fixed 页面、修改路由或管理导航选中态。页面完成 React 化后，应同步删除对应注册桥、页面 CSS 与 DOM 增强代码。

### 修改应落在哪一层

| 要修改的内容 | 唯一职责位置 | 禁止做法 |
| --- | --- | --- |
| Demo 时间常量 | `009-0-demo-time.js` | 散落到页面模板或 CSS |
| 云盘示例数据 | `009-1-data-drive.js` | 在视图打开时另拼第二份数据 |
| 供应链项目、任务、人员数据 | `009-2-data-supply.js` | 为某个页面复制一套项目对象 |
| 团队消息、AI 身份、OpenClaw session 示例数据 | `009-3-data-im.js` | 在 DOM 模板中硬编码消息或会话 |
| 补丁注册、锚点替换公共能力 | `009-4-registry.js` | 新增另一份 patch 数组、Observer 或全局替换器 |
| 统一 IM 的数据适配及业务组件接入 | `009-5-patch-im.js` | 为关注、最近、我的 AI 分别重画消息区和输入框 |
| 非 IM 的通用运行时适配 | `009-6-patch-general.js` | 把明确属于侧栏或自动化的修改塞进来 |
| 一级导航、侧栏与路由状态 | `009-7-patch-sider.js` | 用 DOM class、模拟点击或手改 `aria-current` 维护选中态 |
| 自动化任务页面适配 | `009-8-patch-automation.js` | 在其他入口覆盖一层自动化页面 |
| 构建、顺序执行与唯一产物 | `tools/build-runtime.mjs` | 在浏览器读取、拼接或编译兼容运行时 |
| 非 React 页面迁移桥 | `010-native-page-registry.js` + 对应页面模块 | 向 `document.body` 追加一级页面、用 fixed/z-index 盖住 Outlet |
| Eva 页面样式 | 对应的 `prototype/*.css`，优先复用现有 token/组件 | 在 JS 里追加页面级 style、靠更高优先级掩盖旧规则 |
| 云端批注 | `review/` 与 `supabase/` | 混入 Eva 产品信息架构或占用产品内容区域 |

### 架构不变量

1. DOM 中只能有一个 Eva 应用实例、一个可见 IM 会话实例和一个一级导航状态源。
2. `009-5` 至 `009-8` 只能经 `window.__evaPatch` 注册补丁；只有 `tools/build-runtime.mjs` 可以读取并装配兼容运行时，浏览器只加载构建产物，禁止读取、拼接、编译 `eva-legacy-runtime.js`。
3. 同一种产品能力只能有一个业务组件。入口差异必须表现为数据和配置差异，不能复制 DOM、CSS 与事件。
4. 路由是一级导航选中态的唯一权威源；业务数据是会话、任务和文件内容的权威源。不得再把 DOM 当状态仓库。
5. 锚点变化必须显式失败，禁止“找不到就跳过”或用更宽泛的字符串替换继续运行。
6. 调整文件或加载顺序时，同步更新 `index.html` 与 `prototype-manifest.json`；二者必须描述同一条链。
7. 一个能力只保留当前入口、状态、CSS 和监听器；不保留平行实现或无调用方代码。
8. 一级页面只允许挂载在 Router Outlet 的路由宿主中；Modal、Popover、右键菜单和批注侧栏可以使用 Portal，但不能承担页面导航。
9. 当前兼容运行时约 18.6 MB；降低首次解析时间需继续将业务域组件化并代码分割。架构状态必须如实描述。

### 改动后的验证

按 CONTRIBUTING.md 的验证分级执行，复用当前代码状态已通过的结果。UI 与交互验收使用本地 HTTP 测试版；涉及路由、导航、IM、弹窗或运行时装配时执行受影响入口的往返切换，确认状态与交互没有残留。静态检查不能替代适用的浏览器验收。

## 当前工作稿与演示数据

- 唯一页面入口为 `index.html`，业务模块位于 `prototype/`，仓库只维护这一套模块化页面源码；`prototype-manifest.json` 是源码职责与装配顺序清单，与 `index.html` 由合同检查保持一致。修改补丁链后必须运行 `node tools/patch-hash.mjs` 与 `node --check dist/vendor/eva-runtime.module.js`。
- 项目仓库 `https://github.com/labilio/eva-evolve`，线上评审入口 `https://eva-evolve.vercel.app/`。本地在仓库根目录 `npm start`，端口按 worktree 路径自动派生（不假定固定 4173）。
- Git 与发布流程唯一依据为根目录 CONTRIBUTING.md：小改动直接推 main，Vercel 自动部署，日常仅本地测试版验收（不主动处理或等待 Vercel）；禁止强推、删除 main 或覆盖他人修改；复杂改动与上游同步的评审、版本号和验证分级按该文件执行。
- 多 agent 并行时一个任务一个 worktree；端口冲突由 serve.mjs 自动让位解决，禁止结束或抢占其他进程监听的端口（不得 `lsof`/`kill` 占用方）；报告 URL 一律以启动输出为准，提到具体 worktree 时一并给出其预览端口或 URL（`npm run ports` 可查全部映射）。
- `vendor/eva-legacy-runtime.js` 是当前构建兼容依赖，不是 Eva 产品或设计参照。AionUI 与 Eva 没有产品关系；新增能力不得照搬或参照 AionUI。
- 项目群聊的现行结构为“大群 → 可选子区”；数据模型、标题、筛选和管理入口都必须遵循这一层级。
- 所有需要预置人员和演示内容的功能，统一在“供应链运营协同”项目（协作项目 ID：`prod`；任务看板项目 ID：`p-supply`）中准备和串联，复用该项目及现有人员的稳定 ID；不得另建默认演示项目、复制人员或项目对象。新增功能的演示说明和 Edge 验收路径默认从该项目开始，必须预置足以走通主流程的内容，不能要求评审者先创建其他测试项目或手工补齐人员；预置只用于原型演示，不得绕过归属、成员添加或项目/群访问规则，跨项目权限测试仅用隔离测试夹具。详细约定见 [演示数据约定](docs/演示数据约定.md)。

## AI 身份统一规则

通讯录、消息、我的 AI、AI 小队、项目成员与选择器、详情和编辑器等入口统一适用。

### AI 头像：全局单一数据源与几何

- AI 头像的主体是一张圆形主图，所有身份都不叠加第二张角图：云端分身与个人助理默认以同一圆形 Eva Logo 为主图（不随主人更换头像变化；历史数据把 Logo 存为自定义主图时按未自定义处理）；无主人的 AI（数字员工、项目管家、外部）沿用各自已确认的头像配置能力，同样保持单张圆形主图。不再有右下角 Eva 小图，也不再要求「拥有人的 AI 角标恒显示」。
- 所有 AI 统一调用 `window.EvaAIIdentity.avatar` 渲染。头像是 AI 身份的全局属性，上述所有入口必须读取同一份头像数据；React 与旧 HTML 只做渲染适配，禁止业务入口自行拼接或替换头像。
- 入口只传整体 size；几何由 `prototype/003-ai-identity.css` 统一计算，业务 CSS 禁止覆盖头像的尺寸比例、圆形裁切或 `object-fit`，也不得自行拼接第二张图。
- 数字员工默认头像复用既有 Lucide Bot 素材（与一级菜单同形），保留素材内留白，不放大图标；统一使用公共头像组件的浅灰底。自定义头像仍遵循本规则。

### 更换头像与圆形裁切编辑器

- 人类本人、分身主人和个人助理主人可更换头像；数字员工与项目管家头像固定、不提供更换入口，也不提供预设或程序生成的头像。姓名不通过头像入口修改；写入只允许本人账号，非法图片数据一律拒绝。
- 所有上传本地头像图片的入口都先经同一个圆形裁切编辑器（可缩放拖拽，页脚「取消 / 保存」）确认后写回，取消不改变头像；入口为各自身份资料卡头像（本人／分身／个人助理）、侧栏底部账号菜单「更换头像」（仅本人）及群头像本地选图，复用同一编辑器、不新增数据源。编辑器不提供「恢复默认」，仅群头像保留「恢复默认头像」。
- 头像数据是各身份的唯一数据源，按身份写入成员档案／主人档案／`EvaAITeam` 配置（具体路径与写入边界详见下述文件），统一经 `EvaAIIdentity.avatar` 渲染。
- 编辑器规格、入口形态、实现文件位置与宿主约束详见 [身份头像更换规则](docs/身份头像更换规则.md)；修改任何头像上传入口前必读，禁止另写第二套裁切（该文件约束与本节同效）。

### AI 标

- AI 标记的用途是在联系人与 AI 共用的身份体系中区分两者，不是所有 AI 的装饰，也不是已连接 Octo 的状态标。是否展示由使用场景决定，身份类型来自数据，不按名称猜测。AI 标紧跟姓名，长名保持单行截断并保留 AI 标的可见空间。
- 显示：团队 IM（包括与 AI 的私聊）、通讯录、项目／群成员列表及成员选择器中的 AI 身份名称旁；「我的 AI」的身份行作为团队 IM 对象入口沿用此规则，即使当前筛选只展示 AI 也保持一致；数字员工市场列表、详情和加入项目等具体身份展示同样显示。
- 不显示：个人「Eva 同学」的助理列表、消息、标题及创建／编辑页面，即使该助理已连接团队也不例外；纯 AI 管理表单（连接助理、创建／配置分身）也不重复显示，进入团队 IM 或成员体系后再显示；群聊标题、session 标题、导航、分类标题及普通内容。
- 头像表达 AI 身份并保持全局一致，AI 标表达协作身份类型；两者独立，不因隐藏 AI 标而改变头像。AI 标记统一复用 Octo `AiBadge`，其底层委托 `window.EvaAIIdentity.badge`；旧 HTML 调用同一公共函数。禁止手写标签或增加颜色、尺寸变体。

### 身份命名与归属

- 云端分身：每人零个或一个，名称固定为「xxx的 AI 分身」，由稳定 ownerId 对应的真人姓名推导，不可独立修改。会话标题、消息署名、添加成员、成员列表与提及候选只展示身份名称及公共 AI 标，不附加灰色 `@主人` 或主人第二行；仅身份资料卡显示可点击的「所属人：姓名」。输入提示和 session 名不追加主人；消息正文中的正常 @ 提及保持原行为。详见 [分身主人展示规则](docs/分身主人展示规则.md)。
- 不以删除历史会话或草稿实现单分身；重复创建与名称限制须同时由数据层保证。个人助理、数字员工和项目管家不受单分身限制，也不套用个人分身归属。
- 通讯录保持联系人／分身两列，一人一行；删除分身数量和展开控件，空值显示「未创建」。内容容器自适应规则见《通讯录身份卡验收》。
- 数字员工不提供「接入配置」入口、页面或占位流程。
- 旧项目助手身份统一称为「Eva 项目管理专员」，历史消息、引用、提及与列表摘要保持同名；统一使用 `EvaAIIdentity.projectAgentAppearance()` 与公共头像组件，不使用随机头像或个人归属头像。各项目 AI 显示名称为「项目完整名称 · 项目管家」，间隔号两侧各一个半角空格，直接拼接，不自动缩写、不使用大模型起名；名称由公共 `EvaAIIdentity.projectAgentName(project)` 从所属项目数据推导，身份 ID、权限、头像及项目配色保持原规则，消息、提及、引用和成员展示使用同一名称。无项目归属的历史群管家按「群完整名称 · 项目管家」展示，子区沿用父群名称；仅调整显示名称，不新增项目归属或扩大成员权限。

### 成员身份展示

- 项目及其群聊的进人、管人界面，凡展示具体联系人或 AI 身份，必须同时展示头像与姓名；覆盖添加候选、已选项、成员列表、角色转让与接任者、移除确认、分身管理及提及成员。不得仅为主列表补头像而遗漏弹窗和记录。
- 联系人统一复用成员身份组件和稳定账号头像，角色置于姓名第二行，不加角色 Tag；紧凑候选及发起人使用同一组件的紧凑尺寸。AI 统一调用 `EvaAIIdentity.avatar` 与 `EvaAIIdentity.badge`；不得在业务入口重画头像或 AI 标。
- 群名、项目名、统计数字和规则说明不属于具体人员身份，不需要附加人员头像。
- 新增或修改入口时，同时检查候选、选中结果、历史记录和确认弹窗，验证头像与实际身份 ID 一致；新增身份入口必须运行头像合同测试，并验证默认主图（分身与个人助理为 Eva Logo）、单张圆形裁切与 AI 标记的全局一致性。

## 「我的 AI」与个人 Eva

入口边界与治理细节以 [我的 AI：Step 1 实施规格](docs/我的AI-Step1实施规格.md) 和 [个人 Eva 文件夹与单助理规范](docs/个人Eva文件夹与单助理规范.md) 为准（其约束与本节同效），核心不变量：

- 「我的 AI」位于个人模块并紧跟 Eva 同学；中栏不显示「AI 小队」「AI 助理」顶层分组标题，AI 小队列表在上、AI 单聊在下，仅以一条横线分隔；AI 单聊按云端分身、个人助理、数字员工分组展示其子区承载的会话。
- 默认「我的 AI 小队」动态包含本人及全部可用 AI，不可编辑、不可解散；自定义小队保存成员快照，名称、头像、成员与「解散 AI 小队」均在父群右上角三点的聊天信息面板管理（加号只增删 AI 成员），中栏小队行不保留第二套治理菜单。解散后删除其消息、草稿、子区及未读状态，回到默认「我的 AI 小队」父群且不保留已删除小队的选中态，不影响 AI 单聊；数据按 AI 小队隔离。
- AI 单聊使用独立 IM 两人群／子区，不复用本地聊天记录。仅个人助理身份行提供「编辑配置」，复用个人 Eva 的共享编辑器；云端分身和数字员工不显示该入口。
- 个人「Eva 同学」主入口只有一个固定 Eva，不展示助理选择，不提供创建或编辑入口；个人对话按文件夹组织，“默认”也是完整分类，输入框可下拉选择文件夹。「我的 AI」可新建多个个人助理，与个人 Eva 共用助理配置数据源，但 IM 消息和草稿独立保存。

### 群聊 AI 发言触发

- 团队 IM 的群聊及子区中，普通 AI 分身、数字员工仅在被明确 @ 后回应，不因普通消息、进展更新或时间到达而主动插话。演示预设同样如此：每次回复必须有对应的联系人 @ 请求，不得为展示富文本插入未经请求的总结。
- 私聊及我的 AI 的“本人＋单一 AI”两人群和其子区中，用户直接发送即触发该 AI，不要求额外输入 @；该例外是固定且唯一的交互，不提供“必须 @ 才回应”的设置，不得扩大到普通项目群。AI 不因普通时间事件或自己的回复继续触发。详细实现见 [AI 小队子区会话方案](docs/我的AI-子区会话方案.md)。
- 项目管理专员是上述限制的例外：可以主动同步项目事项并 @所有人，包括项目欢迎、项目级提醒与进展汇总；仍须遵守项目及群的数据可见范围，不替联系人作出对外承诺。

## IM 内核、输入提示与图标

- IM 内核唯一实现是 `EvaIMConversation` 及其 Octo-Web 消息组件和交互控制器。“消息－关注”“消息－最近”“个人－我的 AI”的右侧消息流、消息操作和输入区只能传入不同数据与入口配置，不得复制 DOM、复制 CSS、重写 `.ch-stream.innerHTML` 或另挂一套事件监听器。
- 三类入口的数据对象必须分开：消息－关注/最近组织 Octo 团队会话；个人－我的 AI 按“AI 小队／AI 身份 → 群或本人与该 AI 的两人群 → 子区会话”组织；个人－Eva 同学是直接使用本地 OpenClaw 的个人多 session 入口。三者可以复用消息能力，但不得把 AI 身份、Octo channel 与 OpenClaw session 混成同一种对象。个人－Eva 同学保留个人助理会话形态，不因团队 IM 统一而强制改造。
- Octo-Web 只提供成熟 IM 能力；Eva 继续拥有自己的导航、信息架构、品牌、文案和业务场景。消息行、连续消息、头像、时间、Hover、右键菜单、多选、引用、文件、任务卡片和输入区必须使用统一 IM 内核的组件合同；发现差异时修复公共实现或适配层，不在单一入口叠加视觉补丁。
- 所有 IM 输入框直接遵循 Octo-Web `ChatComposer.buildPlaceholder`，共用 `evaIMPlaceholder`，不得由业务入口另写文案或模板。有对象名称时显示 `发送给 xxx`：「发送给」后恰好一个半角空格再接对象名称，不追加省略号、冒号、快捷键或其他说明；名称缺失时按 Octo 原逻辑显示 `发送消息`。
- 输入提示覆盖联系人私聊、AI 助理私聊外观入口、群聊、子区及子区侧栏；对象名称从当前会话数据推导，切换或改名后同步更新，不缓存第二份名称。我的 AI 的 AI 助理私聊使用 AI 身份名称，不泄露底层两人群或子区名称；AI 小队使用 AI 小队名，其子区使用子区名。离线、禁用或其他状态沿用状态组件及发送权限，不通过替换输入提示表达；不得回退为“在 xxx 中回复…”等入口特有文案。（参考：Octo-Web `c2e2aeed2d7027e3aa25f8c726798eb7cdba2463` 的 `ChatComposer.buildPlaceholder` 与 `messageInput.placeholder.directWithName|direct` 文案。）
- Eva 自有且需要持续维护的页面外壳、导航、弹窗、表单、按钮、列表和空状态优先使用现有项目封装；没有封装时使用 Semi UI。不得为了技术栈统一而用 Semi UI 重造行为完整的 Octo IM 业务组件。
- 所有应用图标继续使用既有 Lucide 组件与语义。Lucide 图标允许重复使用：同一语义应优先复用同一图标，不同语义只在确实会造成识别混淆时调整；禁止把“每个图标只能出现一次”当作规则。禁止手写 SVG、Unicode 图形或 CSS 图形替代已有 Lucide 图标；若当前 bundle 没有所需导出，使用项目既有的 `createLucideIcon` 和 Lucide 官方节点定义创建组件，并保留标准 Lucide 类名、尺寸和 `currentColor` 行为。
- 聊天区（气泡、引用卡、系统提示等）的暗色适配遵循 [IM 暗色适配规范](docs/IM暗色适配规范.md)。IM 内核消费 `--wk-*` / `--semi-*` 自有命名空间并含大量写死的浅色值，不随 Eva 语义 token 自动进入暗色：新增或修改聊天区元素时，必须在 `prototype/057-gds-dark-tokens.css` 内一次补齐该元素的表面、边框、文字、图标及全部交互态，只补其中一项会造成白字白底。交互态按 GDS §3.4 遮罩翻转用叠加实现，不得为暗色另挑实色。

## UI 状态归属与迁移规则

- 新增或修改 UI 状态前，必须明确三个信息：唯一所有者、可推导的数据源、组件切换时的重置边界。能够从路由或业务数据推导的视觉状态，不得再保存第二份 DOM、`window` 全局变量或 `body` class 状态。
- 一级导航的唯一权威源是 URL／路由。选中态只能从当前路由推导；禁止通过“先点击另一个导航，再用覆盖层改结果”、手动互斥 `aria-current` 或捕获阶段拦截点击来模拟导航。
- IM 的入口模式由 `evaMessageMode` 决定，消息－关注/最近与个人－我的 AI 各自的数据由 `messageSource(evaMessageMode)` 提供，并统一交给 `ChannelsView`；模式变化必须通过组件身份或公共状态控制器重置内部会话状态，不得让上一模式的选中会话残留到下一模式。
- DOM 增强脚本只能补充 React 未承载的独立 Demo 表面，不得把 DOM、MutationObserver、定时器或自定义事件作为 React 页面主状态。不得在一级导航或统一 IM 上新增捕获监听与 `stopImmediatePropagation()` 旁路。
- 每项能力只允许一个入口、一个数据源、一组 CSS 状态选择器和一个事件控制器；无调用方代码必须删除。
- 涉及导航、路由或 IM 数据源的改动，交付前必须运行语义合同检查及最终生成 ES 模块语法检查。检查应验证状态归属和禁止的旁路模式，不使用脱离业务域的全局数量阈值代替架构判断。

## 客户端窗口外壳

- Eva 同学是桌面客户端，不是网页产品。原型中的 `.topbar` 表示客户端系统标题栏，是所有页面共享且必须持续可见的窗口外壳，始终保持唯一可见。
- 新增或修改消息、Space、云盘、通讯录等一级页面时，只替换标题栏下方的内容区；不得从视口 `top: 0` 覆盖标题栏，也不得为某个页面复制、重造或省略标题栏。
- 一级页面禁止使用全屏或 `position: fixed` 根节点覆盖现有应用，只能由 React Router 选择并挂载到原生 Outlet 中的唯一页面宿主；页面内部标题属于内容标题，不能替代系统标题栏。
- 弹层、抽屉和遮罩也要先判断其归属范围。除非明确评审的是整个窗口级阻断态，否则默认限制在标题栏下方的客户端内容区。
- 交付前静态检查所有新增一级页面根节点，确认系统标题栏中的品牌、Token、帮助、反馈及窗口控制不会被页面内容遮挡。静态检查只能证明源码边界正确，不能表述为已经完成视觉或交互验收。

## 视觉基准与公共控件

### 客户设计规范（GDS for AI 2.0）

- 规范随仓库保存在 `docs/design-system/gds-for-ai2.0/`，是个人 Eva 桌面体验的设计依据；AionUI 和常见 AI 聊天产品都不能替代。开始 UI 改动前必须先读该目录 `README.md`、`design.md`、tokens、组件合同与 `assets/reference/` 状态截图并运行 `validate.mjs`，核对各项一致，不能只看一张图近似手搓。
- 现行已验证基准（视口、骨架、主列、主色与圆角分级等）以目录内 `design.md` 与 tokens 为准；生成中与完成态属于同一任务生命周期，不得拆成彼此无关的平行页面。
- GDS 只适用于个人 Eva 体验；团队 IM 仍以 Octo-Web 成熟 IM 能力为标准，三者边界不得混淆。GDS 通过语义 token 和组件适配层接入，不把原始色值散落到业务 CSS。

### 三栏式视觉一致性

- 适用于个人「Eva 同学」、团队「消息」、「我的 AI」及后续采用“功能导航／会话列表／内容区”三栏结构的页面。相同语义的元素必须保持宽度、字号、字重、文字色、背景、边框、圆角、间距及悬停／选中样式一致。
- 现行视觉基准是用户确认的同事三栏调整版（2026-09-07，main 提交 a6213ed），个人 Eva 使用该版本页面，以 [三栏式现行视觉规范](docs/三栏式现行视觉规范.md) 为准；不得再以历史 336px 中栏、13px 名称等旧标准回退已认可样式，也不得以“保持旧基准不变”为理由撤销。
- 导航宽度、中栏宽度与拖拽范围、中栏变量与底色/边框等数值以现行视觉规范为准，不自行取平均。
- 复用当前样式、组件及语义变量；后续修改公共 token 仍需核对所有入口，不能为单页叠加高优先级补丁。
- 统一的是外观，不是功能或信息结构。不得因此增删摘要、按钮、未读／提及提示，或更改分组、session、子区、折叠默认值、导航、会话切换和数据模型。AI 标的场景差异仍遵循上方 AI 身份统一规则。
- 右侧内容区沿用各自业务组件：团队 IM 复用统一内核，个人 Eva 保留个人体验；不能因同为三栏就把个人页面改成团队 IM 或复制 IM 实现。
- 验证须在 Edge 的同一视口与缩放下对比目标页和两处基准页，检查宽度、文字层级、颜色、截断及悬停／选中态，并执行入口往返。静态检查不能替代视觉验证；报告必须说明基准页是否被修改。

### 项目配色管理

- 修改项目图标、项目背景、群头像或项目管理专员头像前，必须阅读并遵循 [项目颜色规范](docs/项目颜色规范.md)。
- 项目 colorKey 是唯一配色数据源，统一调用 `window.EvaProjectAppearance`；不提供用户选色，不按项目名称推断颜色，不在业务入口另建色板或自行混色。项目颜色由当前业务对象直接推导，不允许 DOM 脚本从标题或历史 localStorage 再取一套颜色。
- 项目图标、页签背景/边框、默认群头像及项目管理专员头像底色保持同一项目色系：子区继承父群项目，自定义群头像优先，无项目归属保持原有外观。新增入口同时验证跨项目切换、旧数据兼容及无项目回退。

### 列表留白、切换与搜索控件

- 列表内容区左右外留白遵循 [列表留白与操作对齐规范](docs/列表留白与操作对齐规范.md)：滚动条占位不参与对称计算，不给左侧补空槽；行内层级缩进独立计算。相关改动运行 `npm run test:layout` 验证实际浏览器几何。
- 明确的视图切换器遵循 [切换控件动效规范](docs/切换控件动效规范.md)，复用公共动效 token；首次显示不播放切换动画。弹窗选项、查询分类、筛选及表单页签不批量套用，保留原有反馈和业务状态归属。
- 新增或修改搜索框、搜索图标按钮前，必须阅读 [搜索控件规范](docs/搜索控件规范.md)；两种入口均不显示快捷键提示。搜索外观统一由 `prototype/055-search-controls.css` 管理，页面只定义布局；使用公共 Lucide Search，禁止私自增加边框、阴影或图标间距。保留原有查询范围、清空行为、禁用状态和入口形态；不因视觉统一新增搜索能力。修改公共搜索样式或接入新入口时，构建后运行 `npm run test:search`（已接入 GitHub quality gate）；新增入口补入搜索浏览器回归清单。

## 云端批注与评审

### 数据存储边界

- 当前 Supabase 仅服务于共享批注，不承载 Eva 云盘文件、聊天消息、项目、任务或人员等产品业务数据。产品业务使用仓库预置演示数据与浏览器本地状态；界面中的“云盘”“共享”等产品概念，不代表原型已接入真实云端存储或跨设备同步。
- localStorage、IndexedDB 属于浏览器本地存储，与 Supabase 独立；引入 IndexedDB 不等于接入云端数据库，也不提供多人共享。
- 排查问题时必须说明涉及批注服务、业务演示数据还是浏览器本地存储，区分现有代码缺陷、原型限制与新增能力建议；不得笼统称为“数据库问题”，也不得因原型限制自行扩展 Supabase 的职责。
- 本地上传是否保存真实文件、刷新后是否保留，应由对应业务规范及本次已确认范围明确；未实现时如实说明，不得用演示样本冒充用户上传内容。本条不自动授权新增文件持久化能力。

### 批注操作与验收

- 机器批注入口与参数规范位于 [docs/AI_COMMENTS.md](docs/AI_COMMENTS.md)，接手批注任务前必须阅读并按其执行（署名、批注类型、状态流转与人工确认规则以该文件为准）。使用 `npm run comments -- add|list|reply|status` 直接操作共享批注，禁止用鼠标自动化模拟填写表单。
- 用户要求“批注／评审但不改代码”时，AI 只能写批注，不得修改产品源码；每条批注必须带页面路径及定位信息，创建前先查询该页面避免重复意见。
- 原型批注是独立评审工具层（源码 `review/`，数据库迁移 `supabase/migrations/`），不是 Eva 产品 UI；不得把批注列表塞进 Eva 页面布局或劫持“反馈问题”入口。

## 聊天信息管理的移植边界

范围与边界以 [聊天信息管理：本轮范围](docs/chat-settings-scope.md) 为准（与本节同效），核心原则：Octo-Web 是交互参考、不是功能清单，只接入已确认且当前原型支持的能力；直接添加成员、无审批或接受机制，被添加不自动带入分身；打开信息面板不重建聊天流与输入框，保留草稿和滚动位置，不覆盖系统标题栏。

## 会话内项目任务

- 会话任务只指所属项目的 Loop 任务；项目群及子区按父群解析项目并检查访问权限，所有私聊、个人 Eva 和非项目群均不提供该入口。
- 实施会话任务入口、消息转任务、创建表单或任务回流前，必须阅读并遵循 [会话内项目任务规范](docs/会话内项目任务规范.md)。复用统一 Loop 创建能力，保留项目前缀及任务/项目数据隔离，不把分派给 AI 等同于启动执行。
