# Eva Evolve 协作与发布流程

本仓库为 `labilio/eva-evolve`，线上为 https://eva-evolve.vercel.app/ 。旧仓库 `labilio/eva-demo-progress` 的发布纪律不自动覆盖本仓库。开工先读 `AGENTS.md` 和相关业务规范。

## 小改动直接发布

允许小改动直接推送 `main`，不强制创建 PR。用户提出的小范围文案、样式、兼容修复或配置修改，默认授权完成必要检查后 commit / push；不重复索要上线确认。用户明确说“不 push”“只预览”或“先评审”时遵从。

1. 检查 `git status`，不得覆盖他人未提交内容。多人或多个 AI 同时工作时使用独立工作区。
2. `git fetch origin`，从最新 `origin/main` 开始；可以在干净 main 上开发，也可以在独立分支完成后直接推送到 main。
3. 按下方验证分级执行检查；有失败不能声称通过。
4. 产品界面或功能发布运行 `npm run release:bump` 更新版本号；纯文档、测试及开发流程配置修改不更新产品版本号。唯一版本数据源为 `release.json`。
5. 推送前再次 fetch。若远端 main 已前进，先逐项整合并重新验证受影响内容，再普通 push；禁止强推覆盖。
6. push 后核对远端 commit 和 GitHub 质量检查。Vercel 自动部署，不手动部署、不轮询部署、不以线上验收作为日常交付前提。

## 多工作区并行与合并顺序

多个 worktree、多个 AI 会话同时开工时，隔离的是工作目录、未提交改动、`dist` 与本地测试版；
**不隔离共享的 `origin/main`**。同一分支只能串行推进：谁先 push，谁就推进了远端，后 push 的人
必然先收到 non-fast-forward 拒绝。这不是冲突、不是失败，也不代表用错 Git，只是"远端已前进"。

- 被拒的 push 不会丢任何东西：本地分支与提交都在，`git fetch` 后 `git rebase origin/main`
  把你的提交重新落在新 tip 上，再 push 即可。同一条分支上的提交按提交顺序重放，只有**改到同一段行**
  才会产生需要人工判断的冲突；改的是不同文件或不同位置时，rebase 会安静通过。
- 缩小窗口比事后救火便宜：一个功能验证完就 fetch → rebase → 跑门禁 → push，不要等几个工作区
  "都做完了再依次推"。分支活得越短、离 main 越近，整合成本越低。
- 需要一次合多个成果时，固定由一个人或一个会话串行执行：一次只整合一个分支，每合一个就跑一次
  `npm test` 与 `npm run check:project`，通过后再 push，然后再合下一个。
- 同步他人的工作区要在**那个工作区里**执行 `git pull`；禁止从外部用 `update-ref`、`reset --hard`
  等方式移动别的会话正在使用的分支指针，那会让对方工作区出现"文件被删"的假象。
- 合并后必须重跑门禁，而不只是看 diff：Git 只合并文本、不合并意图，`maskClosable` 这类一行之差的
  产品取舍会在没有任何冲突提示的情况下被带回旧值。此类关键取舍应固化为断言
  （参见 `docs/弹窗失焦关闭规范.md` 的回归护栏一节），让它变成红灯而不是等到验收才被发现。

## 大改动与可选 PR

跨模块改造、权限/身份边界调整或需要评审的工作，继续使用功能分支、本地测试版和 PR。同步旧仓库仍先生成同步 PR，处理冲突及验收后再合并；不自动覆盖新仓库。复杂发布保持人工明确确认合并。

PR 是评审方式，不是所有修改的必经步骤。每个 PR 需提供目标 commit、本地验收结果（涉及 UI 时附本地地址）。使用中文提交信息，不混入无关文件。

## 必要保护

- GitHub 禁止强推和删除 `main`，管理员同样受约束。
- 不配置强制 PR 或推送前必须已有 CI 结果的门槛，以允许直接 push。
- 每次 push main 自动运行 Eva quality gate；CI 与 Vercel 独立运行，CI 是推送后的检测，不是上线前的阻断。失败后立即检查原因，必要时用 revert 提交回退，不用 reset/force push。
- 不直接修改旧仓库；通过普通 Git 合并吸收上游，不使用整文件 ours/theirs 处理业务冲突。
- 不上传令牌、密码、私有环境文件；前端只使用允许公开的配置。

## 状态汇报

日常只汇报实际完成的本地检查、本地浏览器验收和 Git 推送状态。未查询 Vercel 时不声称已部署；无需为了补齐状态而查询部署或打开线上页面。

## 验证分级与本地测试版

- 纯说明文档：检查差异、链接和 `git diff --check`，无需全量测试、构建或浏览器。协作规则、检查脚本及 CI 配置修改还需运行对应测试与 `npm run check:collaboration`。
- 产品源码：开发中只运行受影响测试；准备推送时集中运行一次 `npm test`、`npm run build`、`npm run check:manifest`、`npm run check:project` 和生成模块语法检查。路由或 IM 改动补充消息路由合同；设计系统材料修改补充 GDS 验证。
- `check:project` 已包含侧栏选中态合同，不重复单独执行。`patch-hash` 仅用于补丁链修改或产物差异排查，不作为所有改动的门槛。
- 同一代码状态已通过的检查可复用结果；只有新修改、上游整合、失败或新的疑点才重跑相关检查。不得跳过失败或删除有效的业务回归断言。
- UI/交互改动直接启动本地 HTTP 测试版并在浏览器验收；文案/局部样式只检查目标表面，共享组件、导航与 IM 改动覆盖受影响入口及往返切换。不为小文案改动执行无关表单、任务等全套操作。
- 首次运行 `npm start`（自动构建），端口按 worktree 路径自动派生（同一路径跨重启稳定，被占用时自动向上让位），以启动输出中的 URL 为准；`EVA_PORT=端口 npm start` 可显式指定。同一 worktree 已有服务在跑时 `npm start` 直接复用并打印地址，不重复启动。已构建且产物未变化时可用 `node tools/serve.mjs dist` 避免重复构建。`npm run ports` 查看全部 worktree 的名字/端口/分支映射；`npm run demo <worktree名或分支片段>` 自动启动并打开对应预览，`npm run demo --stop <同参数>` 只停该 worktree 自己的服务。确认 HTTP 服务可访问后再提供地址；源码变化后重新构建，不能验收旧 dist。禁止为释放端口结束其他进程（`lsof`/`kill` 抢占端口）。
- Vercel 保持 Git 自动部署。仅用户明确要求线上验收，或正在排查线上部署问题时，才查询和操作 Vercel；无需为日常开发创建或打开 Vercel Preview。

发布与验证流程只在本文件维护；其他规范引用本文件，不重复定义门槛。
