# IM 左右气泡初步试验

结论：基本成功，可以沿当前方向继续。无需更换 IM 产品或重做后端；后续工作集中在呈现细节和完整业务回归。本结论针对 Eva Evolve 原型，不代表真实 Octo 服务端联调已经完成。

## 本地交付

- 分支：`codex/im-bubble-trial`
- 工作区：`/Users/secret/Documents/GitHub/eva-im-bubble-trial`
- 基线：`7806f335782ac85f9f1053a8ce2acd897aa8b601`（09-09 v15）
- 测试版：`http://127.0.0.1:4183/#/messages`；重启命令 `EVA_PORT=4183 npm start`。
- 入口：关注 → 供应链运营协同 → IM 气泡验证 → 三个子区。
- 未推送、未合并、未发布。原工作区的未提交文件未改动。

## 实现与边界

`009-5-patch-im.js` 将当前身份 ID 传给已有 MessageRow，以 ID 判定本人消息。消息类型只用于选择展示样式；原 TextContent、MarkdownContent、文件卡、事件处理继续复用。

`017-im-shell.css` 实现右侧浅蓝/左侧浅灰、短消息收缩、连续消息对齐和宽内容内部滚动。文件卡不重复套文字气泡。头像、AI 标、时间、输入框、导航和个人 Eva 页面沿用现行实现。

`009-3-data-im.js` 提供测试群内容；`009-2-membership.js` 沿用成员存储的一次性增量初始化，创建 prod 项目下的群和三个子区。复用现有成员与项目管家，不另建项目、人员或后台存储；刷新不重复灌入，删除后不重建。

未修改任何后端源码、API 请求、协议、Markdown 解析/净化逻辑或消息传输结构。原型业务使用浏览器本地数据；没有用真实服务器验证端到端兼容。

## 已验证

- Edge 1200×800：原消息入口、最近/关注、个人 Eva、我的 Agent 与团队子区往返。
- 本人右、其他人/AI 左；短消息、连续消息、系统分隔与表情。
- 原 Markdown 渲染器处理双方标题、粗体、引用、列表、代码与表格；1000px/1200px 视口无聊天流横向溢出。
- 实际发送消息、右键菜单打开、信息面板开关保留草稿、最近/关注切换保留草稿。
- 测试群三个子区、图片加载和放大/关闭、四种文件卡呈现、Markdown 文件真实预览和关闭。
- 刷新后测试群及消息数量不重复；浏览器测试未捕获页面 JS 异常。
- `npm test`：328 项通过。
- `node --test tests/browser/im-bubbles.mjs`：通过（共享 IM 场景综合回归）。
- build、manifest、project、消息路由合同、IM 外壳合同、patch-hash、生成模块语法检查、GDS validate 通过。

截图在本地 `artifacts/im-bubble-trial/`（未纳入 Git）。自动化阻止外部请求，不读写共享批注。

## 需要继续完善的事项

1. 输入框换行：基线与试验版均使用 textContent 读取 contenteditable；浏览器生成的 DOM 换行可能丢失。已对基线运行相同填充与发送，得到“第一行第二行”。Markdown 布局测试通过注入含真实换行的输入草稿再点击发送验证；不能据此宣称粘贴/中文输入法已验收。
2. 长代码的原有语法高亮在浅底上部分颜色偏淡；下一轮应单独检查代码主题的对比度，保持 Markdown 安全处理。
3. 回复/转发/多选等原型中已有占位行为未被扩展，本次仅证明菜单和原消息控制器没有另起实现；完整真实 IM 能力仍要在正式客户端验证。
4. 真实 AI 流式更新、海量历史、滚动锚定、失败重发、离线与真实服务端兼容未作为本次通过项。初步试验完成后应进入这些重点回归。
5. 预置图片 URL 使用初始化时本地站点 origin；换端口的已有浏览器数据不是跨站持久化合同。固定使用本交付端口查看。

建议：继续当前路线，先让客户确认这版气泡密度、颜色和宽度，再处理代码可读性、输入换行及正式 IM 回归。不需要继续大范围寻找替代 IM。

## 消息操作专项验收（用户补充）

右键菜单、转发、多选是后续必须完成的功能验收，不能用“菜单出现”代替。

| 能力 | 当前已核实状态 | 完整验收要求 |
| --- | --- | --- |
| 右键菜单 | 左右消息复用原菜单；已验证可打开 | 两侧、连续消息、文件卡均定位正确，不越过窗口边界；关闭后恢复正常交互 |
| 复制 | 现有处理器调用 clipboard.writeText | 实读剪贴板，核对原文与 Markdown；失败不得假报成功 |
| 回复/引用 | 现有原型仅 Toast 提示“已进入回复” | 选中原消息、显示引用、取消、发送并保留引用关系 |
| 转发 | 现有原型仅 Toast 提示“已打开转发” | 选择合法目标、确认、目标会话收到对应内容，取消不发送 |
| 多选 | 现有原型仅 Toast 提示“已进入多选” | 两侧复选框位置一致、选择/取消、批量动作、退出及切换会话清理状态 |
| 撤回 | 现有原型仅提示“演示消息暂不撤回”，入口按姓名判断 | 按稳定身份与权限判断，确认后消息状态真实更新 |

以上占位行为不是气泡改造引入；但也不属于功能验收通过项。初步样式试验成功不能据此升级为完整 IM 改造完成。接入这些能力时继续复用 Octo 的消息控制器和现有数据合同，避免单独为气泡写另一套业务流程。
