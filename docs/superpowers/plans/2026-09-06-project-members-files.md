# 项目成员、全员群及文件共享实施计划

目标：按《项目与群聊成员体系》完成可点击的原型主流程，继续现有 Semi UI、Lucide 风格。用户已授权实施，不使用个人 GDS 重画。

架构：唯一成员数据控制器管理联系人、分身、邀请和群关系；React 组件挂入现有项目设置、群信息、创建项目与文件操作入口。复用统一 IM，不新增一级页面覆盖层。原型本地持久化，不宣称真实服务端鉴权或 SSO 已接通。

- [ ] 成员控制器：项目创建、全员群派生、邀请审批与接受、分身归属、普通群范围、退出与转让；先行为测试后实现。
- [ ] Semi UI 成员组件：项目设置入口、群信息入口、邀请记录、带入分身多选、分身详情与演示身份切换。
- [ ] 运行时接入：创建项目及群列表读同一控制器；模式切换重置局部状态，移除旧成员操作入口。
- [ ] 文件：群文件来源与任务信息，预览共享范围后转存项目，共享版本可访问，源群历史权限保持独立。
- [ ] 最低合同、构建语法及测试；Edge 验证项目 → 其他入口 → 项目，以及邀请、接受、移除、转存主流程。
- [ ] 仅在功能分支开发与验证，提供本地预览；用户明确确认可以合并分支前，不创建 PR。正式发布还须确认，不提前更新版本或合并 main。

验收：联系人和分身分开计数；只有本人带入分身；全员群不能独立改成员；普通群必须本人接受；子区继承父群；移除级联、历史保留；文件转存不公开原群内容。复杂并发服务端实现、SSO、云端资源管理不在本轮。

## 预置与演示路径（最新约定）

后续需要预置人员和内容的部分统一在“供应链运营协同”（协作项目 ID：`prod`；任务看板项目 ID：`p-supply`）中准备，遵循 `../../演示数据约定.md`。成员、分身、邀请、全员群、普通群、子区与文件共享的演示应在该项目中串联。下方“成员流程验证”等项目仅为历史临时测试记录，不作为后续默认演示入口；创建项目和跨项目边界可单独使用临时测试数据。

## 最新选择规则（覆盖下方历史记录）

2026-09-06 用户确认：带入分身是可选多选项，默认空数组，不选即可创建或加入。移除独立“不带入”选项、必选星号和空选拦截。下方曾要求明确选择的内容仅为历史记录，已不适用。邀请收件人、转让接任者及补充分身操作仍需选中实际目标。

## 本地执行记录 2026-09-06

已接入项目设置成员管理（复用运行时 Semi Table / Modal / Select / Button），移除旧 MembersTab 直接添加成员的实现。创建项目已要求明确选择分身。业务控制器本地持久化；邀请、审批、接受、移除、转让与分身归属有行为测试。通讯录适配排除 ai / robot / inactive。

Edge 在 http://127.0.0.1:4174/#/collab 实测：进入项目设置成员页；打开邀请候选人；发现旧人员数组包含 AI，已补适配及回归测试；新建“成员流程验证”，未选择分身时没有创建，选择本人分身后成功，成员页显示 1 位联系人 / 1 个分身。截图确认标题栏可见，沿用原有浅灰 Semi 风格。邀请完整闭环、修正后候选人复测、切换入口返回及弹窗遮罩仍待验收。

尚未完成：全员群接入统一 IM 列表；普通群创建/成员入口；全局按演示身份过滤项目内容；退出时强制移交群主的流程；文件来源与转存共享；全部流程最终 Edge 验收。不要将本阶段构建通过当作整项完成。

本地服务 session 38901，端口 4174。Edge 测试 tab 791529654（成员流程验证项目 sp-mtpke5b3），仅测试本地存储，不是后端权限。未 commit / push / PR，未改 main 或 release。

### 群列表接入阶段

已加入 store.channels() 派生全员群和成员可访问的普通群；项目 ChannelsView 传入 membershipProjectId，仍使用统一消息内核。channelsOfSpace 同时提供团队消息里的过滤来源。SpaceFrame 订阅成员状态，未加入项目只渲染自己的邀请处理界面，隐藏任务/文件内容。群信息用同一个 GroupPanel/成员管理组件；项目设置默认打开成员标签。新增 GroupInvitations 以便未入群的人接受邀请。移除项目成员时可在同一确认弹窗指定其各群接任者，独自所在群自动解散，事务先校验再修改。

Edge 已实测旧测试项目出现全员群、人数 1 联系人 / 1 分身、统一输入框，群信息说明同步范围；创建“采购工作群”选择不带入分身后列表即时出现、选中和输入区对应新群，群信息显示 1 联系人 / 0 分身。尚未实测最新邀请收件入口/接任者流程。创建弹窗截图发现标题栏被遮罩变暗，已调整 CSS 选择器，待最终截图复测。

后续重点：非项目群入口仍是旧创建流程，需替换为同一控制器；全局消息页对成员变更的订阅/普通群入口需要补全；@all 候选人过滤与新子区权限注册尚需接入；文件来源及转存共享还未实施。整体未完成，未创建 PR。

### 文件转存阶段

已新增 `009-1-file-sharing.js`，文件共享记录独立持久化，权限读取成员控制器。IM 文件右键菜单“转存到项目”展示目标项目人数、分身数及不会开放原群聊天的说明；生成按来源/版本去重的共享副本。FilesView 仍复用 FileList 和 FilePreviewHost，显示来源/共享版本、支持预览下载，共享版本不暴露旧编辑菜单。

Edge 本地实测：供应链运营协同 → 群聊 → 质量与排产 → A-2409 PDF 右键 → 转存到项目 → 选择供应链运营协同 → 范围显示 5 联系人/0 分身 → 确认后出现已转存 → 团队文件中出现共享版本。发现 toEntry 覆盖结构化 source，已修复并添加执行构建后适配函数的回归测试。刷新后重新进入文件页，仍显示共享文件及“来源：质量与排产”。打开预览发现原来的 PDF 只有占位文本，新增标明演示用途的真实 PDF 静态样例，已用 Poppler 渲染检查无布局/字体问题，已接入 demoFileUrl；最后这版真实 PDF 的 Edge 预览/下载仍待验证。

新增静态样例 `prototype/assets/file-samples/a-2409-demo.pdf`，生成脚本 `tools/generate-file-demo.py` 使用 ReportLab + macOS Arial Unicode，应用构建无需 Python。Bundled Python /Users/secret/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 可运行，系统 Python 没装 reportlab，不必再装。

接下来：完成非项目群与全局消息接入、提及及子区权限注册；完整邀请/退出级联浏览器验收；文件转存跨成员权限及真实样例预览下载验收。当前仍未 commit/push/PR/上线。Edge tab 791529669，服务器 session 38901 / 4174。

### 统一非项目群与移除旧旁路

已接入非项目群（store.channels(null, actor)）及全局消息订阅；移除运行时中旧的 GroupCreateDialog/归属弹窗/直接加人处理器与候选人状态。新增消息持久化、子区详情持久化及创建/更新权限登记。主输入区与子区输入区以 actor+conversation 为组件身份重置，提及按钮打开 Semi 选人弹窗，候选人来自群成员，全体成员人数只计联系人。

Edge 发现 `023-demo-0902-v2.js` 仍在全局消息插入另一套直接加人的 DOM 群创建 UI；已删除该整个旧模块，从 index 和 manifest 删除入口，并清理 022 中对应样式、016 对原生创建按钮的隐藏，以及项目目录订阅旧“群自动变项目”数据的逻辑。消息合同检查改为验证 React 的 AI 模式隔离与禁止旧 DOM 创建层。36 项测试通过。

Edge 复测全局消息 → 原生加号 → 新建群聊，已显示统一 Semi 表单，截图确认系统标题栏保持明亮可见，遮罩从标题栏下方开始（此前 Semi 不支持 wrapClassName，现统一用 className）。当前 tab 791529669 停在新建非项目群弹窗，已 markHandoff。

仍需最终完整验收：非项目群实际创建/邀请/接受；项目普通成员审批链；移除/退出级联；子区消息持久化与提及；真实 PDF 预览下载；入口来回切换。代码尚需检查旧群信息的无效治理行（非项目群也应隐藏）、删除子区的持久化处理与 @all 的通知目标模型。目前未 commit / push / PR / 上线。

## 供应链预设及主演示验收 2026-09-06

新增供应链专用预设（协作范围 prod，不是任务看板 p-supply），明确确认后载入，保留其他项目；3 联系人/2 分身，何静待审批，整改群及子区、消息、SC-103 PDF 已预置。教程见 ../../供应链运营协同-演示与验收教程.md。Edge tab 791529692 实走审批→项目接受→文件转存→未进群读取共享 PDF→群邀请接受→子区发言→退出级联→历史保留；刷新及消息/项目切换验证通过。修复 PDF MIME、预览宿主尺寸、动态子区排序时间、历史邀请离开状态文案。38 项测试及最低合同检查通过。最后重新载入预设，Edge 保留演示起点。服务器 session 7146，端口 4174。未 commit/push/PR/main/部署。
