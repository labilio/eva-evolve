# 群聊治理可操作化实施计划

1. 在成员业务仓库加入群治理状态、父群解析、有效管理员合并和四类写入 API。
2. 用单元测试覆盖手动与自动管理员并存、权限边界、Bot 管理员、免 @、GROUP.md、刷新恢复和成员清理。
3. 在聊天信息页接通群管理员与 Bot 管理员选择器、撤销操作、回复规则开关和 GROUP.md 编辑。
4. 更新现行产品规则，删除“仅展示、不可操作”的旧约束。
5. 用浏览器回归验证按钮可操作、结果即时展示、关闭重开及刷新后仍保留。

