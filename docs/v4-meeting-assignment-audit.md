# v4 会议分工核对

> 历史审计记录：下文描述 2026-09-06 至 2026-09-07 的实现与验证，不作为现行功能清单、尺寸规范或发布状态。数字员工「接入配置」已取消；现行三栏以 [三栏式现行视觉规范](./三栏式现行视觉规范.md) 为准。

## 整合后验收（2026-09-07）

- 本地构建通过；全量测试 114/114 通过。manifest（75块）、项目合同、消息路由、导航选中态、运行时哈希、最终模块语法、GDS规范校验和 git diff --check 均通过。
- 真实浏览器走通新建 Loop → 看板 SC-108 → 详情：目标、完成标准、真实负责人保存正确，初始状态待办；新任务显示“暂无评论”和“暂无执行记录”。临时任务已重新加载清理。
- 修复运行历史、评论、子任务的任务/项目隔离，避免继承其他示例记录；隔离专项4/4，任务创建9/9，表单交互7/7。
- 数字员工市场更多菜单只含“放进项目”“接入配置”，无“拉进群”；表格无“一句话”列。AI 团队工具栏支持创建云端分身与添加数字员工；分身私聊没有项目 Loop 入口。
- 员工选择器姓名13px，AI 标紧随姓名，间距6px。设置保留原菜单并新增用量看板；1280×720下弹窗宽1240、高643.6，左20、上56.4，标题栏完整，无文档横向溢出。先前1200×800下亦已检查布局。
- 本轮最终浏览器复验使用本地内嵌浏览器，不等同于全量 Edge 验收；没有声称逐个市场条目或真实后端服务已联调。
- 整体 UI 已局部统一员工表格密度、336px中栏盒模型、身份名称布局、弹窗边界及表单间距。复用既有身份组件、语义变量和 Semi；未改写全局 GDS token，未反向重做个人 Eva、通讯录或团队 IM 基准。
- 下表仍标注“部分/未完成”的会议事项没有据此宣称完成。任务及附件目前保留在原型页面内，非服务端持久化。
- **仅本地已修改；未 commit、未 push，GitHub main 与 Vercel 均未发布本轮变更。**

审计日期：2026-09-06；更新验证：2026-09-07。对象为当前本地工作区，包含未提交修改。依据 `/tmp/eva-v4-meeting.txt` 的会议转写；时间为录音相对时间。此表是源码与定向测试审计，不能替代浏览器验收，也不代表 GitHub 或 Vercel 已更新。附带原型是参考材料，会议中的明确决定及当前用户要求优先。

“完成”指已有对应代码；视觉一比一、实际端到端均需主任务的浏览器证据。会议没有提到任务前缀恢复，该项来自用户后续直接要求，单列补充。

| 会议时间 | 分工或决定 | 状态 | 当前证据与差距 |
| --- | --- | --- | --- |
| 00:00:30–01:18 | 个人工作区：Eva 同学、任务看板、自动化任务、连接中心；移除我的分身 | 部分 | `prototype/009-7-patch-sider.js:76` 的个人数组顺序正确，`:83` 名称为任务看板/自动化任务/连接中心且无我的分身；分组仍写“个人”，未对齐会议的“个人工作区”。 |
| 00:01:08–01:33 | 第二分组叫团队协作，第一项消息 | 完成 | `prototype/009-7-patch-sider.js:76` 消息第一项，`:83` 分组团队协作。 |
| 00:02:41–03:47 | 非项目群及私聊使用独立分组；关注按右键关注组织 | 部分 | `prototype/009-5-patch-im.js:258`、`:260` 接入非项目群与私聊分类。`:197` 仍从 `loadSpaces()` 生成全部项目会话，不读取置顶项目集合；当前源码不能证明关注只展示用户关注内容。会议中“非项目群”的最终替代名称尚未定。 |
| 00:04:43–06:22 | IM 任务是 Loop；本地助理留个人，个人体验和运行过程不用重做 | 完成 | `prototype/009-5-patch-im.js:197` AI session 单独数据对象；`:317`–`:319` 项目 Loop 按 membership 项目归属、群与项目双权限显示，不给私聊。项目页 query 接入在 `prototype/009-6-patch-general.js:92`、`:223`。旧独立个人待办 `037`/`038` 已删除。 |
| 00:06:51–07:19 | 工作板内容无需重做，只调整相关入口 | 完成 | `prototype/009-7-patch-sider.js:83` 显示任务看板，保持原路由 `/eva-stub/工作板`；未据此重造任务业务。 |
| 00:08:30–09:19 | Agent 创建中心、数字员工市场完整迁入，分类与点击链路都要可用 | 部分 | `prototype/047-digital-employees.js:55`–`:80` 有配置页、分类、市场、详情等入口；`:103` 授权/测试/断开仍有模拟提示。迁入完整性由另一专项对源原型逐项核对；本审计不把“代码有按钮”当作端到端可用。 |
| 00:09:34–10:05；13:57 | 用量看板与设置迁入；设置可放底部 | 完成 | `prototype/048-settings-usage.js:22`–`:25` 用量概览、范围、业务域、明细和对话入口；`:30`–`:31` 设置菜单含用量看板，宽度接近全屏；按用户后续要求采用自有 UI，非逐像素复制。视觉待验收。 |
| 00:10:05–10:52 | 连接中心移个人；连接器/MCP 归属另行对齐 | 完成 / 暂缓 | 导航移动见 `prototype/009-7-patch-sider.js:76`、`:83`。连接器与 MCP 个人/公共授权归属在 17:15 明确“先不动”，不应擅自补成新规则。 |
| 00:11:26–13:05 | 仅项目管理员或负责人可配置、放入数字员工；普通成员无权 | 未完成 | `prototype/009-2-membership.js:38` 的 `addEmployee` 只检查人类与成员身份，没有 `manager` 检查。`prototype/047-digital-employees.js:25` 项目候选只筛 `canRead`，不是管理员项目。已用隔离内存夹具确认普通成员 `manager=false` 仍成功加入员工，见下方复现记录。 |
| 00:11:39 | 删除数字员工“拉进群”，保留放进项目 | 完成（源码与专项测试） | 用户随后明确确认此项。`prototype/009-2-membership.js:38` 现只接受项目 ID，`all:项目ID` 归一化到项目；项目内普通群、无项目群均拒绝，已有成员数据不迁移、不清除。`prototype/047-digital-employees.js` 的市场、详情、聊天及弹窗已删除拉进群入口与处理分支；只保留放进项目。 |
| 00:12:05 | 普通用户只看到对应业务域专家，拥有全局权限者看全部 | 未完成 | `prototype/047-digital-employees.js:68`–`:72` 是查询与业务域手动筛选；未建立 actor 到可见业务域的权限数据与过滤规则。筛选控件不能替代权限。 |
| 00:12:24–12:38 | 市场和项目内都能加入数字员工，均遵循管理员权限 | 部分 | 市场侧有 `prototype/047-digital-employees.js:71` 的放进项目；项目成员选择器 `prototype/009-2-members-ui.js:64` 只组合人类和 cloneItems，未加入 employee 候选。市场权限本身尚缺管理员校验。 |
| 00:13:20–14:24 | 左侧一级导航对齐；通讯录保留自有业务，呈现形态参考原型 | 部分 | 导航主体已迁入但“个人工作区”文字仍缺。通讯录 `prototype/033-contacts-redesign-v2.js:93`–`:120` 为现有联系人/AI 分身双列列表；尚无与参考原型同视口的视觉验收，不能声称形态已对齐。 |
| 00:14:40–15:50 | POC 不做组织架构，先平铺；消息、通讯录不放搜索框 | 部分 / 暂缓 | 组织架构明确暂缓，现有平铺符合决定。IM 搜索移除见 `prototype/009-5-patch-im.js:305` 等；通讯录 `prototype/033-contacts-redesign-v2.js:93`、`:167`–`:176` 仍有可展开搜索入口，尚未移除。 |
| 00:17:15–结尾 | 连接器/MCP 先不动；阅读后续概念文章和 MOPT，另一人接力 | 暂缓 | 连接器归属未定，属于待澄清而非本轮可自行设计的缺口。后续文章和 MOPT 不在此次提供的会议转写中，不能仅凭会议声称已逐句落实。 |

## 用户后续补充

| 事项 | 状态 | 证据与限制 |
| --- | --- | --- |
| 我的 AI 支持直接加入数字员工 | 完成（源码） | `prototype/009-5-patch-im.js` 的 EvaAITeamPage 已有员工选择与分组 session；`prototype/047-digital-employees.js:71` 有加入 AI 团队操作。此处是个人收藏/协作对象，不应推导为有权限配置项目员工。 |
| 任务必须保留项目前缀，支持完整编号查找 | 完成（源码与定向测试） | `prototype/009-6-patch-general.js:183`–`:210` 恢复前缀设置、格式及跨项目冲突检查；`:222` 新建任务按项目生成完整编号，`:224` 接受完整 identifier 查找。旧任务编号不重写。旧调用缺少 issuePrefix 的兼容已修复，项目设置测试全部通过，新增完整编号创建与详情查询测试亦通过。 |

## 定向执行证据

2026-09-07 重跑 `node --test tests/project-info.test.mjs tests/project-members.test.mjs tests/digital-employee-project-scope.test.mjs`：27 项全部通过。此前 issuePrefix 缺参导致的两项失败已修复，storage full 路径恢复覆盖。

新增 `tests/loop-task-create.test.mjs` 通过 createPatchedRuntime 提取真实 createIssue/getIssue，再以 Node VM 隔离业务数据验证。9 项全部通过。首轮发现的跨项目看板归属与负责人伪造名称/类型两项问题已修复并重跑：其他项目不会接受 p-supply，负责人显示信息从成员、分身、员工或项目专员身份数据派生。覆盖跨项目归属、角色限制、父任务、完整编号、详情、附件和不继承旧 mock 数据。

对管理员边界使用 Node VM 的独立内存夹具（无磁盘数据写入）：创建 owner 项目，邀请并接受 member，令组织数字员工存在，调用 `addEmployee('p','member','employee')`。实际结果为 `{"memberIsManager":false,"employeeIds":["employee"]}`，确认普通成员仍可配置项目员工。

本次未运行构建、未提交，也未修改业务源码。并行任务可能继续修改文件，本表应在整合后用最新源码与浏览器结果复核；行号指本次审计时的工作区。

## 数字员工禁止拉进群补充验证

用户明确确认后，数据入口已收紧到项目。执行 `node --test tests/digital-employee-project-scope.test.mjs`，3 项全部通过：项目内群/非项目群拒绝且无状态修改；项目及全员群别名可添加且去重；原项目成员权限保持不变。本次没有增加管理员专属规则，也没有清除已有群成员数据。上述“不修改业务源码”仅指初次审计阶段。
